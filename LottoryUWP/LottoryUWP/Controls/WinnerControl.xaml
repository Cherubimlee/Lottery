<UserControl
    x:Class="LottoryUWP.Controls.WinnerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LottoryUWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="local">

    <Grid>
        <Border Name="borader" BorderThickness="2,2,2,2" CornerRadius="30" Margin="5,8" MinWidth="240" MinHeight="80">
            <TextBlock Foreground="{StaticResource TextBoxForegroundThemeBrush}" FontSize="26" Margin="10" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Run x:Name="R1" Text="{Binding MajorValue, ElementName=local}"/>
                <Run x:Name="R2" Text="{Binding SecondaryValue, ElementName=local}" FontFamily="Segoe UI"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
